- if user_signed_in?
  .row
    %aside.span4
      %section
        = render 'shared/user_info'
      %section
        = render 'shared/advertisement_form'
- else
  - if @advertisement_items.any?
    %ul.thumbnails
      = render :partial => "shared/advertisement", :collection => @advertisement_items
